- Create a `register-person.xhtml` page to register person information
- Create a `register-address.xhtml` page to register address information
- Define the flow in `registration/registration-flow.xml`